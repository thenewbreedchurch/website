{
  "name": "@nb-church/db",
  "version": "0.1.0",
  "private": true,
  "main": "./index.ts",
  "types": "./index.ts",
  "scripts": {
    "generate": "prisma generate",
    "migrate:dev": "prisma migrate dev",
    "migrate:deploy": "prisma migrate deploy",
    "seed": "tsx prisma/seed.ts",
    "studio": "prisma studio",
    "lint": "echo 'no lint configured for db package' ",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@prisma/client": "^6",
    "argon2": "^0.45.1",
    "dotenv": "^17.4.2"
  },
  "devDependencies": {
    "prisma": "^6",
    "tsx": "^4",
    "typescript": "^5"
  }
}
